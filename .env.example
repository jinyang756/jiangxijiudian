#---------- 前端（公开；可在浏览器版本中安全暴露） ----------
# Supabase 项目 URL（来自项目设置 → API）
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase 匿名（公钥）——前端使用安全
VITE_SUPABASE_ANON_KEY=您的匿名公钥

# 可选：如果需要，请提供显式存储 URL（通常 SUPABASE_URL + /storage/v1 即可）。
VITE_SUPABASE_STORAGE_URL=https://your-project-ref.supabase.co/storage/v1

# 本地开发/代理的可选应用路由
VITE_API_BASE_URL=/api
VITE_ADMIN_BASE_URL=/_

#---------- 服务器 / CI / 密钥（请勿提交，请勿添加 VITE_ 前缀）----------
# Supabase 服务角色密钥 — 仅限服务器端，授予完全访问权限（请保密）
# SUPABASE_SERVICE_ROLE_KEY=您的服务角色密钥

# 可选：用于服务器端脚本的直接数据库连接字符串（请保密）
# 示例格式：postgres://postgres:password@db.host:5432/postgres
# SUPABASE_DB_URL=postgres://username:password@host:port/database

# 可选：部署环境
# NODE_ENV=development

#---------- 注意事项和指导 ----------
# 1）切勿提交包含真实密钥的文件。请将 .env 文件添加到 .gitignore 文件中。
# 2) 使用 VITE_ 前缀
# 3) 通过 Secret/Environment 配置将 SUPABASE_SERVICE_ROLE_KEY 和 SUPABASE_DB_URL 注入到您的部署平台（Vercel/Netlify/GitHub Actions/GCP/AWS 等）。
# 4）如果需要运行时检查，请添加启动脚本来验证所需的环境变量：
#    - 对于前端构建：VITE_SUPABASE_URL 和 VITE_SUPABASE_ANON_KEY
#    - 对于服务器：SUPABASE_SERVICE_ROLE_KEY 和/或 SUPABASE_DB_URL
# 5) 如果您的应用使用 SSR，请确保在服务器端渲染期间永远不会将服务角色密钥发送给客户端。

#---------- 项目特定提示（基于当前数据库） ----------
# 您的项目包含以下表格：dishes、categories、orders、transaction_account_ledger、api_keys、operation_logs。
# 运行需要 SUPABASE_DB_URL 或 SERVICE_ROLE_KEY 的迁移/测试脚本时，请考虑将这些服务器端密钥添加到 CI 中。

#----------部署示例命令----------
# GitHub Actions（示例）——在仓库设置中设置密钥：
# SUPABASE_SERVICE_ROLE_KEY -> ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
# SUPABASE_DB_URL -> ${{ secrets.SUPABASE_DB_URL }}
# Vercel / Netlify：
# 在项目环境变量面板中添加 SUPABASE_SERVICE_ROLE_KEY 和 SUPABASE_DB_URL。
#---------- .env.example 文件结束 ----------